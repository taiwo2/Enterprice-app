import React from 'react';
import { makeStyles } from '@material-ui/core';
const ProductForm = () => {
  return (
    <div>
      <h1>ProductCreateForm Works! h1</h1>
    </div>
  );
};
export default ProductForm;

const useStyles = makeStyles(theme => ({}));